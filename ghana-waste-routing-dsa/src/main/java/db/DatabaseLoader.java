package db;

/**
 * Owner: Hannah Aidoo (confirm if this is "Nana Aba")
 * Reads CSV seed files, validates records, loads them into the database
 * and into the custom in-memory structures. Also writes algorithm_runs
 * results back to the database.
 */
public class DatabaseLoader {
    // TODO
}
