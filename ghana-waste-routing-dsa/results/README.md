# Performance results

- `csv/` — raw timing data from your experiments (run each ≥3 times, average them)
- `graphs/` — exported line graphs (runtime vs input size)

Suggested file naming: `<algorithm>_<experiment>.csv` e.g. `binarysearch_vs_linearsearch.csv`
